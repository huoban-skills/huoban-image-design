# 营销类画布（报告配图 / 宣传讲解）

- **画布模式**（默认，不加 fullbleed）：窗口是带圆角细边的卡片，直接浮在透明底上（浏览器预览为白底，嵌入报告/文档则与纸面融为一体），无衬底色块、无投影；四周留白给浮层和标注气泡用。这是"一张图"，不是"一个系统"。
- **底层＋浮层**：`.stage.has-float` 时窗口收窄到 1440，浮层探出补满 1640；无浮层则窗口撑满。浮层左右两侧都可探出：右侧＝默认；左侧＝加 `float-left` 类（窗口右移 200）。**同一批图的浮层位置要错开**，不要张张都贴右缘；按被强调元素的位置就近选边（强调左侧导航/首列就放左）。
  左探出的两条纪律：一是它会盖住左侧导航中段，选纵向位置时让工作区名、当前项和底部条露出来；二是**独立自定义详情页别用左探出**，那类图没有左侧导航、内容满宽，浮层压上去就把标题卡和左列内容挡了。
- 浮层（`.mk-float`）位置任意，但只能**补充底层尚未出现的内容**，可用两种形态：
  1. **新组件示意**：展示底层没有的相关真实组件，如快捷表单、任务清单或第二业务视角。
  2. **点击结果态**：展示底层某个操作触发后、当前画面尚未出现的真实结果态，如弹窗、下拉或提示。
- **禁止重复放大**：不得把底层已有卡片、字段、统计、表格或相同信息复制到浮层中；需要强调底层已有内容时，仅调整底层自身的对比关系，不再画第二份。
- `.mk-callout` 标注气泡只在用户明确要求业务价值标注时添加，不作为营销类默认元素；不得压住组件或替代缺失内容。
- 浮层内容必须是伙伴云已有组件，强调≠虚构；不得为给浮层腾位置而裁短底层窗口，底层计划展示的内容必须完整。
- 导出：window-size = `1704,<stage高+96>`（1640＋body 左右 padding 32×2），**加 `--default-background-color=00000000`** 得透明底 PNG，嵌任何底色都无缝（命令见 [verify-export.md](verify-export.md)）。
