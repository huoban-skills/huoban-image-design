# 2.8.5 演示尺寸底图加宽到 1400

演示尺寸底图 1300 → 1400，整图 1500 → 1600；高度阈值按同样比例换算（目标 778–1000，Medium 到 1120，偏矮 718–777 Medium、717 以下 High）。主体字在 1440 视口 8.5px，1280 视口 7.5px 低于底线，演示要在 1440 以上的视口放。huoban-solution-ppt 的 handoff.md 同步。

数据大屏不放浮层：`kind="screen"` 里写 `hb-float` 时 expand 直接报错说明原因，marketing.md 开头注明。
