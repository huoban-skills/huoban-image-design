# 1.5.1 中央视觉位默认图与客户配图

- `hb-svisual` 不给内容时默认画一张线框地球＋节点连线＋地台光环的内联 SVG，颜色走 `--screen-accent` 随主题变；不画任何真实地图边界。
- `hb-svisual img="路径"` 放客户自己的 3D 厂区图、地图或产品图。
- build.py 组装时把 `<img src="本地路径">` 内嵌为 data URI（相对内容文件目录或当前目录找），单文件仍可再导出；找不到的文件按原路径保留并提示。
