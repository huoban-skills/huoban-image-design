# 2.8.4 版式优先于比例

- 页面原则给的版式和必有组件在演示尺寸下一个都不拆，放不下宁可偏高。底图高目标 722–930（比 1.4–1.8），931–1040 报 Medium 写明理由，高于 1040 才报 High。去掉「看板和工作台在演示尺寸下明细与主图表并排」这条——它改了看板版式的顺序。
- 看板必有的图表指趋势图（柱状／折线／面积／双轴），环图、条形图不能代替：expand 对底图没有趋势图的看板报错，check.py `base-skeleton` 按图表卡的 `data-chart` 判。图表卡新增 `data-chart` 属性。
- marketing.md 重写为「选尺寸 → 定比例 → 排浮层」三节：数据量收成一张下限／上限表作为唯一出处，删掉三处互相打架的行数、四处重复的并排说明和一段历史留档；verify-export.md 的比例阈值改为指向 marketing.md；SKILL.md 浮层一句补上手机单屏。
