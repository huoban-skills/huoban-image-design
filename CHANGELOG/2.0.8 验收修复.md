# 2.0.8 验收修复

> 2026-09-20

## 重点

1. **浮层宽度口径统一成 480～920。** 宏说明、宽度报错和遮挡提示里的 960 都改了，和实际拦截一致；演示尺寸 400～640。

2. **清掉和现行规则打架的旧说法。** 营销画布里的"浮层最多两张卡"、演示尺寸说明里的"三到五块"和超过 5 块的提示都删了；验收表第 1 条不再核对组件列，改核对内容、页面与端、浮层那几块和决定。

3. **浮层写任何 side 属性都报错**，包括 `side="right"`。

## 其他

- README、导出说明里的 `--render` 改成"有浏览器自动做，`--no-render` 关"。
- 验收文档去掉"导出后把 PNG 放到浅色底上看一眼"。
- 渲染检查不再对手机屏幕里的留白报空隙：屏幕固定 812 高，内容短时下方留白是真实样子。
