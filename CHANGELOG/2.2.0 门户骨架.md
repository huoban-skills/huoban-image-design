# 2.2.0 门户骨架

2026-09-21 在 hb.huobanyun.com 的门户上按安卓 UA、375×812 视口实测 H5 真实 DOM，补上手机端的门户几块。门户页仍挂在 `kind="mobile"` 下，不另立页面类型。

## 新增宏（scripts/expand.py、assets/c4-mobile.html、assets/base.css）

- `hb-ptop` 门户顶栏：logo 32 ＋ 门户名 15/500；未登录出 76×32 主色登录按钮，登录后换成 24 圆头像。
- `hb-pnav` 门户导航条：一级页签 44＋1px 底线，选中态只有下方 30×2 主色横条、文字不变色；3 个以内平分整宽，再多按内容宽左起排并横向滚动；分组页签带 ▾ 排在最后。排不下时报警告。
- `hb-pmenu` 门户分组菜单：导航条下压 2px 主色线，白面板垂下，每项 50 高（24 图标 ＋ 名称），行间细线从图标右侧起；面板以下整屏盖 45% 黑蒙层。
- `hb-plogin` 门户登录页：卡 338 宽 r8 居中，logo 40 ＋ 门户名 20/700，手机号、验证码（右嵌 106 宽灰底按钮）、登录按钮（未填写的浅色态）、可选微信登录，卡底 Powered by 伙伴云 ｜ 免责声明 ｜ 投诉。底图是门户可选配置，默认白底。
- `hb-pme` 个人中心：用户卡 343×104（头像 72）＋ 信息卡（行 48 高、行间细线）＋ 退出登录按钮。

`hb-phone` 加 `nodots`：顶栏右侧不出 ···，个人中心这类系统页用。

## 口径

手机端的列表页一律是卡片（`hb-ocards`），没有手机版表格；只有自定义页面里的表格组件在手机上仍是表格。这条写进了手机端宏文档的末注。
