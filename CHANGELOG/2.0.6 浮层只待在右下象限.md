# 2.0.6 浮层只待在右下象限

> 2026-09-18

## 重点

1. **浮层只待在底图的右下象限。** 以底图中心为原点，浮层顶端落在底图半高处，左边不越过底图中线，右缘探出 200，下半截可以探出底图，画布按浮层实际高度自动撑高。浮层仍没有位置属性，模型不用选位置。

2. **遮挡天然不超过四分之一。** 浮层只压右下那一块，宽度上限随之改成 480～920（演示尺寸 400～640）。

## 其他

- 演示尺寸的高度检查改量底图窗口的高度，浮层探出的部分不算。
